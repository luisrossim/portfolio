import { Profile } from "./profile"
import { Projects } from "./projects"

export const Main = () => {
    return (
        <main>
            <Profile />
            <Projects />
        </main>
    )
}